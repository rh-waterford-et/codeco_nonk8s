apiVersion: v1
kind: Secret
metadata:
  name: vk-flightctl-oauth
  namespace: codeco
  labels:
    app: vk-flightctl-provider
type: Opaque
stringData:
  # Replace these values with your actual OAuth credentials
  client-id: "flightctl-codeco"
  client-secret: ""

